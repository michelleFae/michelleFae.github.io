# Soda Meets Spark

Soda io is a platform that performs quality checks on data.

It can be integrated with spark since it treats the data in spark as a SQL datastore. So, it can run QCs (quality checks) on data in spark.

## Rabbit MQ

<embed src="images/soda_meets_spark.pdf#toolbar=0&navpanes=0&scrollbar=0" type="application/pdf" width="100%" height="600px" />